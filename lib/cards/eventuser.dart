import 'package:flutter/material.dart';

import '../model/events.dart';

class EventUser extends StatelessWidget {
  Events user;
   EventUser({super.key,required this.user});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
